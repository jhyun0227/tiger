<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="favoritens">
	<!-- 좋아요 목록 -->
	<select id="list" parameterType="integer" resultType="favorite">
		select f.*, m.MB_id, m.MB_nickName, m.MB_gender from favorite f, member m where f.MB_numT=m.MB_num and
		MB_numG=#{MB_numG} order by FV_num
	</select>
</mapper>