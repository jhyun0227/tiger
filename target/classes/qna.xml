<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qnans">
	<select id="getTotal" parameterType="qna" resultType="integer">
		select count(*) from qna
		<where>
			<if test="keyword!=null and keyword!=''">
				QA_title like '%'||#{keyword}||'%' or QA_content like '%'||#{keyword}||'%'
			</if>
		</where>
	</select>
	
	<select id="list" parameterType="qna" resultType="qna">
		select * from (select a.*, rowNum rn from (select * from qna
		<where>
			<if test="keyword!=null and keyword!=''">
				QA_title like '%'||#{keyword}||'%' or QA_content like '%'||#{keyword}||'%'
			</if>
		</where>
		order by QA_num desc) a) where rn between #{startRow} and #{endRow}
	</select>
	
	<select id="select" parameterType="integer" resultType="qna">
		select * from qna where QA_num=#{num}
	</select>
	
	<select id="getMaxNum" resultType="integer">
		select nvl(max(QA_num),0)+1 from qna
	</select>
	
	<update id="updateStep" parameterType="qna">
		update qna set QA_refStep=QA_refStep+1 where QA_ref=#{QA_ref} and QA_refStep>#{QA_refStep}
	</update>
	
	<!-- 여기부터 하기 -->
	<insert id="insert" parameterType="qna">
		insert into qna values (#{QA_num}, #{QA_title}, sysdate, #{QA_content}, #{QA_ref}, #{QA_refStep}, #{QA_refLevel}, 'N', #{MB_num})
	</insert>
</mapper>
